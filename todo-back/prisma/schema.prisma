generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Todo {
  id          Int     @id @default(autoincrement())
  description String
  done        Boolean @default(false)
}